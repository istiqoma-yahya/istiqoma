## Packages
date-fns | Formatting dates for deed history
recharts | Visualizing good vs bad deed statistics on dashboard
framer-motion | Smooth animations for entry and interactions
lucide-react | Beautiful icons for the interface
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes

## Notes
Authentication is handled via Replit Auth (useAuth hook).
API endpoints are defined in @shared/routes.
Using 'Outfit' as display font and 'Inter' as body font.
